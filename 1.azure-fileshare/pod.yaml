apiVersion: v1
kind: Pod
metadata:
  name: ubuntu-sysbox
spec:
  containers:
  - image: blockchainazure.azurecr.io/ubuntu-sysbox:1
    name: mypod
    volumeMounts:
      - name: azure
        mountPath: /mnt/data
  volumes:
  - name: azure
    persistentVolumeClaim:
      claimName: pvc-azurefile
